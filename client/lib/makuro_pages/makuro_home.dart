import 'package:flutter/material.dart';

class MakuroHome extends StatelessWidget {
    const MakuroHome({
        Key ? key
    }): super(key: key);

    @override
    Widget build(BuildContext context) {
        return const Text('Home');
    }
}